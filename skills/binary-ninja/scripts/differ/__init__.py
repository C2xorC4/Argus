"""Binary diff — Phase 5.

Planned modules (deferred):
    byte_diff.py     — file-level baseline diff
    hlil_diff.py     — semantic edit distance (replaces total_bytes
                        criterion)
    callgraph_diff.py — call-graph + caller/callee deltas
    string_diff.py   — string-table diff (security fixes show up here)
    fix_patterns.py  — pattern library: write-then-verify-cleanup,
                        bounds-check-added, SDDL-tightened, etc.
"""
