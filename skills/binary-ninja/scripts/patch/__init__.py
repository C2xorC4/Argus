"""Binary patching — Phase 5.

Planned modules (deferred):
    nop.py        — NOP-out instructions
    branch.py     — force always/never-jump; conditional inversion
    string.py     — string replacement with padding
    redirect.py   — call redirection
    code_cave.py  — large-payload insertion via cave / detour
    metadata.py   — PE / ELF / Mach-O checksum / signature fixup
    protector.py  — VMProtect / WinLicense / Code Virtualizer-aware
                     unpacking (manual PE mapper, VEH crash capture)
"""
